target/
.fastembed_cache/

# runtime data directories
crawl_data/
crawl_data.high_quality/
lexical_index/

# index artifacts
hnsw_index.bin
wiki_hnsw.bin
*.hnsw.data
*.hnsw.graph

# niche corpus artifacts (large)
*.niche/
*.niche.bin

# local backups / restores / runtime debris
*.local_backup/
migration_backup_*/
modal_restore_tmp/
__pycache__/
.pytest_cache/
.DS_Store

# logs / reports
*.log
crawl_output.txt
reports/

# local TUI dependencies
tui/node_modules/

# local notes
agents.md

# large binaries / reports (not source)
*.pdf
*.docx
*.tar.gz
*.bak
final_report.md
generate_report.py
research.pdf

# runtime database files
CURRENT
IDENTITY
LOCK
LOG
LOG.old.*
MANIFEST-*
OPTIONS-*

# high quality runtime data (large)
hnsw_index.high_quality.bin
lexical_index.high_quality/

# embedding server runtime artifacts
*.bin
!Cargo.lock
!tui/bun.lock

